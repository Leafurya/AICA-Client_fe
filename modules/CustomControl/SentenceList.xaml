<UserControl x:Class="CustomControl.SentenceList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomControl"
             xmlns:viewmodel="clr-namespace:CustomControl.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:SharedViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="288">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <DockPanel>
                <CheckBox x:Name="toggleButtonSelectAll"  MinWidth="88"
                          VerticalAlignment="Center" HorizontalAlignment="Left"
                          Content="전체 선택"
                          Checked="toggleButtonSelectAll_Checked" Unchecked="toggleButtonSelectAll_Unchecked"></CheckBox>
            </DockPanel>
            <DockPanel Grid.Column="1">
                <Button Content="선택 삭제" Width="98" Height="28"
                        VerticalAlignment="Center" HorizontalAlignment="right"
                        Click="Button_Click"
                        Style="{StaticResource RoundedButton}"/>
            </DockPanel>
        </Grid>
        <ScrollViewer Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      Style="{StaticResource ScrollViewTransparent}">
            <StackPanel>
                <ItemsControl x:Name="itemsControl" ItemsSource="{Binding SentenceList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:SentenceItem></local:SentenceItem>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
