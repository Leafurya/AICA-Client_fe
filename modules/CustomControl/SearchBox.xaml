<UserControl x:Class="CustomControl.SearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomControl" xmlns:viewmodel="clr-namespace:CustomControl.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:SharedViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="229.843" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <DockPanel>
            <ToggleButton Name="toggleBtnMode" Width="20" IsChecked="{Binding IsSearchModeToggleOn}"  Checked="ToggleBtnMode_Checked" Unchecked="ToggleBtnMode_Unchecked"/>
            <TextBlock>검색모드</TextBlock>
            <Button Name="btnSelectImg" Content="이미지 선택" Click="btnSelectImg_Click"/>
        </DockPanel>
        <Grid Grid.Row="1" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <RichTextBox Grid.Row="0" x:Name="textBoxSearcher" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextChanged="textBoxSearcher_TextChanged">
                <FlowDocument>
                    <Paragraph>
                        <Run Text=""/>
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>
            <Canvas Grid.Row="0" Visibility="Collapsed" Name="canvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                    Background="Transparent" Cursor="Hand"
                    MouseLeftButtonUp="Canvas_MouseLeftButtonUp" MouseMove="Canvas_MouseMove" MouseWheel="Canvas_MouseWheel">

            </Canvas>
            <TextBlock x:Name="textBlockTranslator" HorizontalAlignment="Stretch" Grid.Row="1" VerticalAlignment="Stretch" Text="{Binding TranslateResult}" TextWrapping="Wrap"/>
        </Grid>
    </Grid>
</UserControl>
